import { BaseBigNumber } from "..";
import { HttpRequest } from "../utils";
export declare class NetworkService {
    httpRequest: HttpRequest;
    constructor(baseUrl: string);
    private createUrlForPos;
    private createUrlForZkEvm;
    getBlockIncluded(network: string, blockNumber: number): Promise<{
        start: string;
        end: string;
        headerBlockNumber: BaseBigNumber;
    }>;
    getExitProof(network: string, burnTxHash: string, eventSignature: string): Promise<any>;
    getProof(network: string, start: any, end: any, blockNumber: any): Promise<any>;
    getMerkleProofForZkEvm(network: string, networkID: number, depositCount: number): Promise<any>;
    getBridgeTransactionDetails(network: string, networkID: number, depositCount: number): Promise<any>;
}
