"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("ethers/lib/utils");
const config_1 = require("hardhat/config");
exports.default = (0, config_1.task)("decode-input-data", "Decodes input data")
    .addParam("inputData", "Input data")
    .addOptionalParam("type", "Function name to decode", "execute")
    .setAction(async ({ inputData, type, env: _env }, { deployments }) => {
    console.log("inputData: ", inputData);
    console.log("type: ", type);
    if (!["execute", "xcall"].includes(type)) {
        throw new Error("Unsupported type");
    }
    const connextDeployment = await deployments.getArtifact("Connext");
    const connextInterface = new utils_1.Interface(connextDeployment.abi);
    const decoded = connextInterface.decodeFunctionData(type, inputData);
    console.log("decoded: ", decoded);
});
//# sourceMappingURL=decodeInputData.js.map