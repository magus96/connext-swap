"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const ethers_1 = require("ethers");
const config_1 = require("hardhat/config");
const utils_1 = require("../src/utils");
exports.default = (0, config_1.task)("propose-transfer-owner", "Propose Transfer Ownership")
    .addParam("newOwner", "New owner")
    .addOptionalParam("connextAddress", "Override connext address")
    .addOptionalParam("env", "Environment of contracts")
    .setAction(async ({ newOwner, connextAddress: _connextAddress, env: _env }, { deployments, ethers }) => {
    let { deployer } = await ethers.getNamedSigners();
    if (!deployer) {
        [deployer] = await ethers.getUnnamedSigners();
    }
    const env = (0, utils_1.mustGetEnv)(_env);
    console.log("env:", env);
    console.log("newOwner: ", newOwner);
    console.log("deployer: ", deployer.address);
    const connextName = (0, utils_1.getDeploymentName)("Connext", env);
    const connextDeployment = await deployments.get(connextName);
    const connextAddress = _connextAddress ?? connextDeployment.address;
    console.log("connextAddress: ", connextAddress);
    const connext = new ethers_1.Contract(connextAddress, connextDeployment.abi, deployer);
    const tx = await connext.proposeNewOwner(newOwner);
    console.log("proposeNewOwner tx: ", tx);
    await tx.wait();
    const proposedOwner = await connext.proposed();
    console.log("proposedOwner: ", proposedOwner);
});
//# sourceMappingURL=proposeTransferOwnership.js.map